From 05f269d1c091f2ad2324cd4969f392e1ba32a5a1 Mon Sep 17 00:00:00 2001
From: Qi Tang <qtang@lanl.gov>
Date: Mon, 6 Apr 2020 15:44:24 -0600
Subject: [PATCH 1/3] periodic bc

---
 deepxde/__init__.py            |  4 ++++
 deepxde/boundary_conditions.py | 42 ++++++++++++++++++++++++++++++++++
 2 files changed, 46 insertions(+)

diff --git a/deepxde/__init__.py b/deepxde/__init__.py
index fd5c56449..a11e426ca 100644
--- a/deepxde/__init__.py
+++ b/deepxde/__init__.py
@@ -9,6 +9,8 @@
 from .boundary_conditions import NeumannBC
 from .boundary_conditions import OperatorBC
 from .boundary_conditions import PeriodicBC
+from .boundary_conditions import PeriodicBC2
+from .boundary_conditions import PeriodicBC3
 from .boundary_conditions import RobinBC
 from .initial_condition import IC
 from .model import Model
@@ -26,6 +28,8 @@
     "NeumannBC",
     "OperatorBC",
     "PeriodicBC",
+    "PeriodicBC2",
+    "PeriodicBC3",
     "RobinBC",
     "IC",
     "Model",
diff --git a/deepxde/boundary_conditions.py b/deepxde/boundary_conditions.py
index f44c91a40..c2ec9abcf 100644
--- a/deepxde/boundary_conditions.py
+++ b/deepxde/boundary_conditions.py
@@ -101,6 +101,48 @@ def error(self, X, inputs, outputs, beg, end):
         outputs = tf.reshape(outputs, [-1, 2])
         return outputs[:, 0:1] - outputs[:, 1:]
 
+class PeriodicBC2(BC):
+    """Periodic boundary conditions (enforce u' and u) along component_x.
+    """
+    def __init__(self, geom, component_x, on_boundary, component=0):
+        super(PeriodicBC2, self).__init__(geom, on_boundary, component)
+        self.component_x = component_x
+
+    def collocation_points(self, X):
+        X1 = self.filter(X)
+        X2 = np.array([self.geom.periodic_point(x, self.component_x) for x in X1])
+        return np.vstack((X1, X2))
+
+    def error(self, X, inputs, outputs, beg, end):
+        mid=beg+int((end-beg)/2)
+        dydx = tf.gradients(outputs[:, self.component : self.component + 1], inputs)[0]
+        dyleft=dydx[beg:mid,self.component_x:self.component_x+1]
+        dyright=dydx[mid:end,self.component_x:self.component_x+1]
+
+        outputs = outputs[beg:end, self.component : self.component + 1]
+        outputs = tf.reshape(outputs, [-1, 2])
+        vleft=outputs[:, 0:1]
+        vright=outputs[:, 1:2]
+        return tf.math.abs(vleft-vright) + tf.math.abs(dyleft-dyright)
+
+class PeriodicBC3(BC):
+    """Periodic boundary conditions (enforce u') along component_x.
+    """
+    def __init__(self, geom, component_x, on_boundary, component=0):
+        super(PeriodicBC3, self).__init__(geom, on_boundary, component)
+        self.component_x = component_x
+
+    def collocation_points(self, X):
+        X1 = self.filter(X)
+        X2 = np.array([self.geom.periodic_point(x, self.component_x) for x in X1])
+        return np.vstack((X1, X2))
+
+    def error(self, X, inputs, outputs, beg, end):
+        mid=beg+int((end-beg)/2)
+        dydx = tf.gradients(outputs[:, self.component : self.component + 1], inputs)[0]
+        dyleft=dydx[beg:mid,self.component_x:self.component_x+1]
+        dyright=dydx[mid:end,self.component_x:self.component_x+1]
+        return dyleft-dyright
 
 class OperatorBC(BC):
     """General operator boundary conditions: func(inputs, outputs, X) = 0.

From 226f077f361c4bfef17a72443613aa117136e493 Mon Sep 17 00:00:00 2001
From: Qi Tang <qtang@lanl.gov>
Date: Mon, 6 Apr 2020 22:24:14 -0600
Subject: [PATCH 2/3] format

---
 deepxde/__init__.py            |  4 ---
 deepxde/boundary_conditions.py | 63 +++++++++-------------------------
 2 files changed, 17 insertions(+), 50 deletions(-)

diff --git a/deepxde/__init__.py b/deepxde/__init__.py
index a11e426ca..fd5c56449 100644
--- a/deepxde/__init__.py
+++ b/deepxde/__init__.py
@@ -9,8 +9,6 @@
 from .boundary_conditions import NeumannBC
 from .boundary_conditions import OperatorBC
 from .boundary_conditions import PeriodicBC
-from .boundary_conditions import PeriodicBC2
-from .boundary_conditions import PeriodicBC3
 from .boundary_conditions import RobinBC
 from .initial_condition import IC
 from .model import Model
@@ -28,8 +26,6 @@
     "NeumannBC",
     "OperatorBC",
     "PeriodicBC",
-    "PeriodicBC2",
-    "PeriodicBC3",
     "RobinBC",
     "IC",
     "Model",
diff --git a/deepxde/boundary_conditions.py b/deepxde/boundary_conditions.py
index c2ec9abcf..ed2cb9cda 100644
--- a/deepxde/boundary_conditions.py
+++ b/deepxde/boundary_conditions.py
@@ -87,9 +87,14 @@ class PeriodicBC(BC):
     """Periodic boundary conditions on component_x.
     """
 
-    def __init__(self, geom, component_x, on_boundary, component=0):
+    def __init__(self, geom, component_x, on_boundary, derivative_order=0, component=0):
         super(PeriodicBC, self).__init__(geom, on_boundary, component)
         self.component_x = component_x
+        self.derivative_order = derivative_order
+        if derivative_order > 1:
+            raise NotImplementedError(
+                "PeriodicBC only support derivative_order 0 or 1."
+            )
 
     def collocation_points(self, X):
         X1 = self.filter(X)
@@ -97,52 +102,18 @@ def collocation_points(self, X):
         return np.vstack((X1, X2))
 
     def error(self, X, inputs, outputs, beg, end):
-        outputs = outputs[beg:end, self.component : self.component + 1]
-        outputs = tf.reshape(outputs, [-1, 2])
-        return outputs[:, 0:1] - outputs[:, 1:]
-
-class PeriodicBC2(BC):
-    """Periodic boundary conditions (enforce u' and u) along component_x.
-    """
-    def __init__(self, geom, component_x, on_boundary, component=0):
-        super(PeriodicBC2, self).__init__(geom, on_boundary, component)
-        self.component_x = component_x
-
-    def collocation_points(self, X):
-        X1 = self.filter(X)
-        X2 = np.array([self.geom.periodic_point(x, self.component_x) for x in X1])
-        return np.vstack((X1, X2))
-
-    def error(self, X, inputs, outputs, beg, end):
-        mid=beg+int((end-beg)/2)
-        dydx = tf.gradients(outputs[:, self.component : self.component + 1], inputs)[0]
-        dyleft=dydx[beg:mid,self.component_x:self.component_x+1]
-        dyright=dydx[mid:end,self.component_x:self.component_x+1]
-
-        outputs = outputs[beg:end, self.component : self.component + 1]
-        outputs = tf.reshape(outputs, [-1, 2])
-        vleft=outputs[:, 0:1]
-        vright=outputs[:, 1:2]
-        return tf.math.abs(vleft-vright) + tf.math.abs(dyleft-dyright)
-
-class PeriodicBC3(BC):
-    """Periodic boundary conditions (enforce u') along component_x.
-    """
-    def __init__(self, geom, component_x, on_boundary, component=0):
-        super(PeriodicBC3, self).__init__(geom, on_boundary, component)
-        self.component_x = component_x
+        mid = beg + int((end - beg) / 2)
+        if self.derivative_order == 0:
+            yleft = outputs[beg:mid, self.component : self.component + 1]
+            yright = outputs[mid:end, self.component : self.component + 1]
+        else:
+            dydx = tf.gradients(
+                outputs[:, self.component : self.component + 1], inputs
+            )[0]
+            yleft = dydx[beg:mid, self.component_x : self.component_x + 1]
+            yright = dydx[mid:end, self.component_x : self.component_x + 1]
+        return yleft - yright
 
-    def collocation_points(self, X):
-        X1 = self.filter(X)
-        X2 = np.array([self.geom.periodic_point(x, self.component_x) for x in X1])
-        return np.vstack((X1, X2))
-
-    def error(self, X, inputs, outputs, beg, end):
-        mid=beg+int((end-beg)/2)
-        dydx = tf.gradients(outputs[:, self.component : self.component + 1], inputs)[0]
-        dyleft=dydx[beg:mid,self.component_x:self.component_x+1]
-        dyright=dydx[mid:end,self.component_x:self.component_x+1]
-        return dyleft-dyright
 
 class OperatorBC(BC):
     """General operator boundary conditions: func(inputs, outputs, X) = 0.

From 2b0e3a9a2ce5bd33bcfeae524e3b3aeed5bcea45 Mon Sep 17 00:00:00 2001
From: Qi Tang <qtang@lanl.gov>
Date: Tue, 7 Apr 2020 13:35:45 -0600
Subject: [PATCH 3/3] format

---
 deepxde/boundary_conditions.py | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/deepxde/boundary_conditions.py b/deepxde/boundary_conditions.py
index ed2cb9cda..1bee0e025 100644
--- a/deepxde/boundary_conditions.py
+++ b/deepxde/boundary_conditions.py
@@ -93,7 +93,7 @@ def __init__(self, geom, component_x, on_boundary, derivative_order=0, component
         self.derivative_order = derivative_order
         if derivative_order > 1:
             raise NotImplementedError(
-                "PeriodicBC only support derivative_order 0 or 1."
+                "PeriodicBC only supports derivative_order 0 or 1."
             )
 
     def collocation_points(self, X):
@@ -102,7 +102,7 @@ def collocation_points(self, X):
         return np.vstack((X1, X2))
 
     def error(self, X, inputs, outputs, beg, end):
-        mid = beg + int((end - beg) / 2)
+        mid = beg + (end - beg) // 2
         if self.derivative_order == 0:
             yleft = outputs[beg:mid, self.component : self.component + 1]
             yright = outputs[mid:end, self.component : self.component + 1]

# PhysicsNeMo Object-A (MeshGraphNet cylinder-flow smoke workflow) reproducibility requirements.
# Tested target: Linux x86_64, Python 3.12, CPU-only execution.
#
# Recommended install command from a fresh virtual environment:
#   python3 -m pip install --upgrade pip
#   python3 -m pip install -r requirements/physicsnemo-object-a-smoke.txt
#
# PyTorch and torch-scatter wheels are resolved from the CPU/PyG indexes below.
--extra-index-url https://download.pytorch.org/whl/cpu
--find-links https://data.pyg.org/whl/torch-2.12.0+cpu.html

numpy
scipy
pyyaml
tfrecord
vtk
wandb
torch
torchvision
torch-geometric
torch-scatter
nvidia-physicsnemo==2.1.1
